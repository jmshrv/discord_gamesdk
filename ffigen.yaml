# Run with `dart run ffigen --config ffigen.yaml`.
name: DiscordGamesdkBindings
description: |
  Bindings for `src/discord_gamesdk.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/discord_gamesdk_bindings_generated.dart'
headers:
  entry-points:
    - 'src/discord_gamesdk.h'
  include-directives:
    - 'src/discord_gamesdk.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
